# Create an app at https://dashboard.privy.io and paste its App ID below.
# Without this, ConnectWalletButton renders disabled and the Live actions
# panel on /app/markets has no wallet to sign with — see providers.tsx.
NEXT_PUBLIC_PRIVY_APP_ID=
