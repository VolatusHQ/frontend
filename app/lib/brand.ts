/**
 * Project name. Change it here and it changes everywhere (nav, headline,
 * metadata, sign-off). The mark is abstract — the identity is the split
 * between calm and wild, not a letter.
 */
export const BRAND = "Volatus";
export const TAGLINE =
  "Liquidity mining is an insurance premium paid in inflation. Volatus turns it into a market.";

/**
 * Where every "GitHub" / "Read the spec" / "Full source" link on the site
 * points. This is the `VolatusHQ` org — `backend`, `contracts` and
 * `frontend` as three separate repos, no single monorepo to prefer one of
 * them over the others — not the private working repo the team actually
 * develops in (`CodeBlocker52/sigma`). A judge clicking through from the
 * live site needs something they can actually open; the private repo 404s
 * for anyone signed out of it.
 */
export const REPO_URL = "https://github.com/VolatusHQ";
