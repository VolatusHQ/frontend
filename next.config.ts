import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev overlay badge sits over the page's lower-left corner and lands in
  // every screenshot taken during a design pass.
  devIndicators: false,
};

export default nextConfig;
